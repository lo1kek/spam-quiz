{
"functions": {
  "api/app.py": {
    "runtime": "@vercel/python",
    "includeFiles": "{templates/**,static/**,config.json}"
  }
},
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "api/app.py" }
  ]
}
