{
  "functions": {
    "api/app.py": {
      "includeFiles": "{templates/**,static/**,config.json}"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "api/app.py" }
  ]
}
